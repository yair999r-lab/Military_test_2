# database

The database of choice is Mongo: for the simple reason that I have no live relationship between tables or am not interested in a defined schema. And of course it is more convenient to store arrays etc. there.